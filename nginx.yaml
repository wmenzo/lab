apiVersion: v1
kind: Pod
metadata:
  labels:
    run: nginx-yaml
    method: fromCode
    another: label
  name: nginx-yaml
spec:
  containers:
  - image: nginx
    name: nginx-yaml
    resources: {}
  dnsPolicy: ClusterFirst
  restartPolicy: Always
status: {}
